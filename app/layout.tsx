import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Agricultural Exports - Premium Farm Products Worldwide',
  description: 'Leading agricultural export company providing premium rice, fruits, vegetables, spices, and grains to global markets with quality assurance and reliable delivery.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
